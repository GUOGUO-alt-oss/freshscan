# 「鲜识 v2.0」系统架构设计文档

> 版本：v2.4 | 日期：2026-06-17 | 关联：01-PRD-v2 / 03-UI设计-v2 / 04-开发计划-v2 | 审查: Claude Code

---

## 0. 模型架构总览（v2 双模型）

```
拍照 → Bitmap
         │
    ┌────┴──────────────────────┐
    │ EfficientDet-Lite0         │  检测阶段
    │ (COCO 预训练, 320×320)     │  → bounding boxes
    └────┬──────────────────────┘
         │ 对每个检测框 crop
    ┌────┴──────────────────────┐
    │ MobileNetV3 (260 类)       │  识别阶段 (Fruits-360 训练)
    │ 输入: [1,224,224,3]       │  → "红富士苹果" / "樱桃番茄"
    │ 输出: [1,260] logits      │
    └────┬──────────────────────┘
         │
    ┌────┴──────────────────────┐
    │ MobileNetV3 (18 类 v1)    │  新鲜度阶段 (辅助)
    │ 输入: [1,224,224,3]       │  → FRESH / ROTTEN
    │ 输出: [1,18] logits       │
    └────┬──────────────────────┘
         │
    DetectedItem { label, freshness, bbox, isCookable }
```

**模型规格:**

| 模型 | 用途 | 类别 | 体积 | 延迟 |
|------|------|------|------|------|
| EfficientDet-Lite0 | 检测框 | COCO 80 类 | 5.2MB | ~80ms |
| MobileNetV3-260 | 品类识别 | Fruits-360 260 类 | ~5.0MB | ~50ms |
| MobileNetV3-18 (v1) | 新鲜度 | 18 类新鲜/腐烂 | 3.6MB | ~50ms |
| **总计** | | | **~14MB** | **~180ms** |

---

## 1. 架构演进总览

```
v1.0 (实时相机)                         v2.0 (拍照识别)
─────────────────                      ─────────────────
CameraX 持续采集 YUV                    系统相机 Intent → Bitmap
   │                                       │
ImagePreprocessor (每帧)                  ImagePreprocessor.resize(320×320)
   │                                       │
MobileNetV3 (每帧)                        EfficientDet-Lite0 (单次检测)
   │                                       │
逐帧 3FPS → StateFlow                    ┌────┴────┐
   │                                     │ NMS 后处理│
稳定性缓冲 (3帧)                           └────┬────┘
   │                                          │
RecognitionResult → UI                  ┌──────┴──────────┐
                                       │ 对每个检测框:     │
                                       │ crop 224×224     │
                                       │ MobileNetV3-260  │  ← 🆕 260类识别
                                       │ MobileNetV3-18   │  ← v1 新鲜度
                                       └──────┬──────────┘
                                              │
                                       List<DetectedItem>
                                              │
                                       粒子动画 2s (并行推理)
                                              │
                                       BottomSheet 三态展开
```

**关键改变：**
- ❌ 移除 CameraX 实时管线 + 逐帧推理 + 3FPS 限流 + 稳定性缓冲
- ❌ 移除 GuideCircle / GuideAnimation / LoadingOverlay / BoundingBoxOverlay
- ✅ 系统相机单次拍照 + 一次性检测
- ✅ 粒子动画覆盖推理等待
- ✅ BottomSheet (ModalBottomSheet) 承载结果交互

---

## 2. 页面与路由

| 页面 | 路由 | 关键参数 | 来源 |
|------|------|---------|------|
| 首页 | `home` | — | 🆕 |
| 分析页 | `analysis?photoUri={uri}` | 拍照 URI（必传） | 🆕 |
| 菜谱详情 | `recipe/{recipeId}` | recipeId（必传） | 🆕 |
| 口味档案 | `profile/taste` | — | 🆕 |
| 购物清单 | `shopping-list` | — | 🆕 |
| 历史记录 | `history` | — | v1 复用 |
| 识别详情 | `detail/{resultId}` | resultId（必传） | v1 复用 |
| 设置 | `settings` | — | 🆕 含 v1/v2 切换 |

### BottomNavigation 结构

```
NavigationBar
├── 🏠 首页 (home)
├── 📜 历史 (history)
└── 👤 我的 (profile/taste)  ← 含子入口：口味/清单/设置
```

---

## 3. 数据层设计

### 3.1 新增数据模型

```kotlin
// 检测结果（领域层）
data class DetectedItem(
    val id: String = UUID.randomUUID().toString(),
    val label: String,              // e.g. "fresh_tomato"
    val displayName: String,        // e.g. "番茄"
    val freshnessLevel: FreshnessLevel,  // FRESH / ROTTEN / UNCERTAIN
    val confidence: Float,          // [0.0, 1.0]
    val bbox: RectF,               // 归一化坐标 [0, 1]
    val isCookable: Boolean        // true for 蔬菜, false for 纯水果
)

// 菜谱（领域层）
data class Recipe(
    val id: String,
    val title: String,
    val category: RecipeCategory,
    val matchIngredients: List<String>,
    val allIngredients: List<Ingredient>,
    val steps: List<CookingStep>,
    val nutrition: Nutrition,
    val cookingTimeMin: Int,
    val difficulty: Difficulty,
    val source: RecipeSource,       // PRESET / LLM
    val isFavorite: Boolean = false
)

enum class RecipeCategory { DIET, HOME, QUICK, SOUP, COLD }

data class Ingredient(val name: String, val amount: String, val isOwned: Boolean = false)
data class CookingStep(val order: Int, val text: String, val timerSec: Int = 0)
data class Nutrition(val calories: Int, val protein: Int, val carbs: Int, val fat: Int, val fiber: Int = 0)

enum class Difficulty { EASY, MEDIUM, HARD }
enum class RecipeSource { PRESET, LLM }
```

### 3.2 Room 数据库扩展

```kotlin
// v1 保留: HistoryEntity + HistoryDao + HistoryDatabase
// v2 新增:

@Entity(tableName = "favorite_recipes")
data class FavoriteRecipeEntity(
    @PrimaryKey val recipeId: String,
    val title: String,
    val category: String,
    val jsonData: String,          // 完整 Recipe JSON
    val favoritedAt: Long
)

@Dao
interface FavoriteRecipeDao {
    @Query("SELECT * FROM favorite_recipes ORDER BY favoritedAt DESC")
    fun getAllFavorites(): Flow<List<FavoriteRecipeEntity>>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insert(recipe: FavoriteRecipeEntity)

    @Delete
    suspend fun delete(recipe: FavoriteRecipeEntity)

    @Query("SELECT EXISTS(SELECT 1 FROM favorite_recipes WHERE recipeId = :id)")
    suspend fun isFavorite(id: String): Boolean
}

@Entity(tableName = "shopping_list")
data class ShoppingItemEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String,
    val amount: String,
    val isChecked: Boolean = false,
    val addedAt: Long = System.currentTimeMillis()
)

@Dao
interface ShoppingListDao {
    @Query("SELECT * FROM shopping_list ORDER BY isChecked ASC, addedAt DESC")
    fun getAll(): Flow<List<ShoppingItemEntity>>

    @Insert
    suspend fun insert(item: ShoppingItemEntity)

    @Update
    suspend fun update(item: ShoppingItemEntity)

    @Delete
    suspend fun delete(item: ShoppingItemEntity)

    @Query("DELETE FROM shopping_list WHERE isChecked = 1")
    suspend fun clearChecked()
}
```

### 3.3 菜谱引擎

```kotlin
// data/recipe/RecipeEngine.kt
// data/recipe/LabelNormalizer.kt — 标签归一化层（v2.4 新增）

class LabelNormalizer @Inject constructor() {
    // 从 labels_v2.txt 的 displayName → 菜谱食材名 的映射
    // 例: "樱桃番茄" → ["番茄"], "红富士苹果" → ["苹果"]
    private val mapping: Map<String, List<String>> by lazy {
        loadMapping()  // 可由 labels_v2.txt 扩展字段或独立 JSON 维护
    }

    fun normalize(fruitLabel: String): List<String> {
        return mapping[fruitLabel] ?: listOf(fruitLabel)
    }
}

class RecipeEngine @Inject constructor(
    @ApplicationContext private val context: Context,
    private val labelNormalizer: LabelNormalizer  // v2.4 新增
) {
    private val presetRecipes: List<Recipe> by lazy {
        loadPresetRecipes(context)  // 从 assets/recipes/preset_recipes.json 加载
    }

    fun recommend(
        items: List<DetectedItem>,
        profile: TasteProfile? = null
    ): RecipeResult {
        val cookable = items.filter { it.isCookable }

        if (cookable.isEmpty()) {
            return RecipeResult(
                recipes = emptyList(),
                note = "检测到的全是水果，建议直接食用 🍎"
            )
        }

        // 标签归一化: Fruits-360 英文标签 → 菜谱中文食材名
        // 例: "Tomato_Cherry_Red" → displayName "樱桃番茄" → 标准化为 "番茄"
        val ingredientNames = cookable.map { item ->
            labelNormalizer.normalize(item.label)  // 返回 ["番茄", "黄瓜", "西兰花"]
        }.flatten().distinct()

        // 按食材匹配数量排序
        val matched = presetRecipes
            .map { recipe ->
                recipe to recipe.matchIngredients.count { it in ingredientNames }
            }
            .filter { (_, score) -> score > 0 }
            .sortedByDescending { (_, score) -> score }
            .map { (recipe, _) -> recipe }

        return RecipeResult(recipes = matched, note = null)
    }
}

data class RecipeResult(
    val recipes: List<Recipe>,
    val note: String?
)
```

---

## 4. 推理管线（v2.4 重写 — 修复坐标空间 + 三阶段管线）

**管线架构（与 §0 一致）：**

```
用户拍照完成 → Bitmap (max 1920px, 以 inSampleSize=2 加载以防 OOM)

AnalysisViewModel.infer(bitmap) {
    launch(Dispatchers.Default) {

        // ═══ 阶段 1: 多目标检测 ═══
        val detectInput = ImagePreprocessor.resize(bitmap, 320, 320)
        val (boxes, classes, scores) = efficientDet.detect(detectInput)

        // NMS 后处理: 去重 + 低置信度过滤
        // score 阈值降至 0.4（考虑到检测模型未在果蔬数据上微调）
        val validBoxes = DetectionPostprocessor.nms(
            boxes, classes, scores,
            iouThreshold = 0.5,
            scoreThreshold = 0.4
        )

        // ═══ 阶段 2: 对每个检测框做品类识别 + 新鲜度分类 ═══
        val items = validBoxes.map { box ->
            // 关键: 坐标反归一化 — box 在 320×320 空间，需映射回原始 bitmap 空间
            val scaleX = bitmap.width.toFloat() / 320f
            val scaleY = bitmap.height.toFloat() / 320f
            val cropLeft  = (box.left * scaleX).toInt().coerceIn(0, bitmap.width)
            val cropTop   = (box.top  * scaleY).toInt().coerceIn(0, bitmap.height)
            val cropRight = (box.right  * scaleX).toInt().coerceIn(0, bitmap.width)
            val cropBottom = (box.bottom * scaleY).toInt().coerceIn(0, bitmap.height)

            // 跳过无效框（宽/高 < 20px）
            if (cropRight - cropLeft < 20 || cropBottom - cropTop < 20) return@map null

            val crop = Bitmap.createBitmap(bitmap, cropLeft, cropTop,
                                           cropRight - cropLeft, cropBottom - cropTop)

            // 品类识别: MobileNetV3-260 (Fruits-360 训练)
            val input260 = ImagePreprocessor.bitmapToTensorBuffer(crop, 224, 224)
            val logits260 = classifier260.classify(input260.buffer)
            val (label, displayName, confidence, isCookable) =
                modelMapper260.mapToLabelInfo(logits260!!)  // 查 labels_v2.txt

            // 新鲜度分类: MobileNetV3-18 (v1 模型复用)
            val input18 = ImagePreprocessor.bitmapToTensorBuffer(crop, 224, 224)
            val logits18 = classifierFreshness.classify(input18.buffer)
            val freshness = modelMapper18.mapToFreshness(logits18!!)

            DetectedItem(
                id = UUID.randomUUID().toString(),
                label = label,                     // "Tomato_Cherry_Red"
                displayName = displayName,         // "樱桃番茄"
                freshnessLevel = freshness,         // FRESH / ROTTEN / UNCERTAIN
                confidence = confidence,            // [0.0, 1.0]
                bbox = RectF(                       // 归一化坐标 [0, 1]
                    box.left / 320f, box.top / 320f,
                    box.right / 320f, box.bottom / 320f
                ),
                isCookable = isCookable             // 来自 labels_v2.txt
            )
        }.filterNotNull()  // 移除无效框

        _uiState.update { it.copy(items = items, isAnalyzing = false) }
    }
}
```

**性能预估（串行，单框 ~130ms；含 NMS ~180ms 总推理）：**

| 阶段 | 操作 | 耗时 |
|------|------|------|
| 检测 | EfficientDet-Lite0 推理 (320×320, CPU 4线程) | ~80ms |
| 后处理 | NMS + 坐标反归一化 | ~5ms |
| 分类 (×N) | MobileNetV3-260 推理 (224×224) + MobileNetV3-18 (224×224) | ~50ms + ~50ms = ~100ms/框 |
| 2框场景 | 总计 | ~80 + 5 + 200 = ~285ms |
| 5框场景 | 总计 | ~80 + 5 + 500 = ~585ms (建议并行化) |

> **优化建议：** 若检测框 > 3 个，将每个框的两个分类任务并行执行（`coroutineScope { async { ... } }`），可将 N 框分类时间压至 ~100ms（与框数无关）。

**2 秒粒子动画完全覆盖所有推理场景。**

### 新增类型定义

```kotlin
// data/inference/model/ModelConfigV2.kt
// 替代 v1 的 ModelConfig @Singleton，支持 260 类
// v1 的 ModelConfig 保留（@Qualifier("v1")），供新鲜度模型使用

data class LabelInfoV2(
    val label: String,         // "Tomato_Cherry_Red"
    val displayName: String,   // "樱桃番茄"
    val isCookable: Boolean    // true = 可入菜
)

class ModelConfigV2 @Inject constructor(
    @ApplicationContext private val context: Context
) {
    // 从 assets/model/labels_v2.txt 加载 260 类映射
    val labels: List<LabelInfoV2> by lazy { loadLabelsV2() }
    val numClasses: Int get() = labels.size  // 260
    // ...
}
```

---

## 5. 状态定义

```kotlin
// 首页
data class HomeUiState(
    val lastScanItems: List<DetectedItem> = emptyList(),   // 上次扫描结果
    val lastScanTime: Long? = null,                        // 上次扫描时间戳
    val selectedCategory: RecipeCategory? = null,           // 当前选中分类
    val recipes: List<Recipe> = emptyList(),               // 菜谱列表
    val isScanning: Boolean = false                         // 是否在拍照中
)

// 分析页
data class AnalysisUiState(
    val photoUri: Uri? = null,              // 拍照 URI
    val isAnalyzing: Boolean = true,        // 是否在分析中（控制粒子动画）
    val items: List<DetectedItem> = emptyList(),  // 检测结果
    val sheetState: SheetState = SheetState.COLLAPSED,  // BottomSheet 状态
    val selectedCategory: RecipeCategory? = null,
    val recipes: List<Recipe> = emptyList(),
    val recipeNote: String? = null
)

enum class SheetState { COLLAPSED, HALF, FULL }
```

---

## 6. 依赖变更

| 依赖 | v1.0 | v2.0 | 变更 |
|------|------|------|------|
| `camerax-*` (4 个) | ✅ | ❌ | 移除，不再需要 CameraX |
| `tensorflow-lite` | 2.14.0 | 2.14.0 | 保留 |
| `tensorflow-lite-gpu` | 2.14.0 | 2.14.0 | 保留 |
| `tensorflow-lite-support` | 0.4.4 | 0.4.4 | 保留（ImagePreprocessor 仍需要） |
| `mediapipe-tasks-vision` | — | 🆕 0.10.21 | 检测模型运行。⚠️ 内部捆绑 TFLite runtime，可能与独立 `tensorflow-lite` 依赖产生符号冲突。建议验证兼容性或统一使用 MediaPipe 内置 TFLite |
| `datastore-preferences` | — | 🆕 1.1.1 | 口味档案持久化 |
| Compose BOM | 2024.05.00 | 2024.09.00 | M3 ModalBottomSheet 等新 API |
| Room | 2.6.1 | 2.6.1 | 保留 + 新增表 |
| `ModelConfig` | @Singleton 18类 | 🔄 重构为 `ModelConfigV2`（260类）+ `ModelConfigV1`（@Qualifier，供新鲜度模型） | v1/v2 模式切换需两个独立配置实例 |

**Dagger/Hilt 变更：** `ModelConfig` 不再作为 @Singleton；拆分为 `ModelConfigV1` (`@Qualifier("v1")`) 和 `ModelConfigV2` (`@Qualifier("v2")`)。`TFLiteClassifier` 增加 `@AssistedInject` 参数指定使用的模型文件路径。

**APK 体积预估：** v1 ~12MB → v2 ~18MB（+5.2MB 检测模型 + ~1MB JSON/资源）

---

## 7. 错误处理矩阵

| 场景 | 表现 | 处理 |
|------|------|------|
| 系统相机无响应 | — | 30 秒超时 → 回到首页 + Snackbar "相机未响应" |
| 拍照失败 | — | onActivityResult(RESULT_CANCELED) → 回到首页 |
| 模型加载失败 | 分析页白屏 | 检测到 `isModelReady == false` → Snackbar "AI 引擎启动失败" + 重试按钮 |
| 检测无结果 (0 框) | 粒子动画结束无食材 | BottomSheet 显示 "未检测到果蔬，请重新拍照" + [重拍] 按钮 |
| 检测结果全水果 | 有框但 isCookable 全 false | BottomSheet 显示食材 + 提示 "建议直接食用" + 无[找菜谱]按钮 |
| 菜谱匹配为 0 | [找菜谱] 后空列表 | LazyColumn 显示 "未找到匹配菜谱，试试其他分类筛选" |
| 拍照权限拒绝 | — | Snackbar "请在设置中开启相机权限" + 跳转系统设置按钮 |
| OOM (大照片) | 崩溃 | Bitmap 采样 `inSampleSize=2`，最大尺寸限制 1920px |
| Process Death | 系统相机期间 Activity 被杀死 | `SavedStateHandle` 保存 `photoUri`，恢复后直接进入分析页，跳过重新拍照 |
| MediaPipe/TFLite 冲突 | 编译或运行时 crash | 优先使用 MediaPipe 内置 TFLite runtime；如冲突则降级为独立 TFLite + 自实现 NMS |

---

## 8. 模型加载策略（v2.4 新增）

### 8.1 加载时机

| 策略 | 优点 | 缺点 | 采用 |
|------|------|------|------|
| Application.onCreate 全量加载 | 拍照时无需等待 | 冷启动 +500-800ms，低端机可能 ANR | ❌ |
| 分析页进入时懒加载 | 不影响冷启动 | 首次拍照等待 1-2s（2s 粒子动画可覆盖）| ✅ **推荐** |
| 后台预加载（首页空闲时） | 兼顾两者 | 实现复杂度高 | 备选 |

**采用懒加载策略：** 用户点击「扫描食材」进入分析页时开始加载模型。2 秒粒子动画足以覆盖模型加载 + 推理时间。

### 8.2 GPU Delegate 分配

三个模型总磁盘 14MB，GPU delegate 下内存更高：

| 设备 RAM | EfficientDet-Lite0 | MobileNetV3-260 | MobileNetV3-18 |
|----------|-------------------|-----------------|-----------------|
| ≥6GB | GPU delegate | GPU delegate | GPU delegate |
| 4-6GB | GPU delegate | CPU (XNNPACK 4线程) | CPU (XNNPACK 4线程) |
| <4GB | CPU (XNNPACK 4线程) | CPU (XNNPACK 4线程) | CPU (XNNPACK 4线程) |

```kotlin
// data/inference/ModelLoader.kt (新增)
class ModelLoader @Inject constructor(
    @ApplicationContext private val context: Context
) {
    private val isLowRamDevice by lazy {
        val activityManager = context.getSystemService(Context.ACTIVITY_SERVICE) as ActivityManager
        activityManager.memoryClass < 256  // < 4GB
    }

    fun createInterpreter(modelPath: String, useGpu: Boolean = !isLowRamDevice): Interpreter {
        val options = Interpreter.Options().apply {
            if (useGpu) {
                try {
                    addDelegate(GpuDelegate())
                } catch (e: Exception) {
                    Logger.w("ModelLoader", "GPU delegate failed, falling back to CPU", e)
                    setNumThreads(4)
                    setUseXNNPACK(true)
                }
            } else {
                setNumThreads(4)
                setUseXNNPACK(true)
            }
        }
        return Interpreter(loadModelFile(modelPath), options)
    }
}
```

### 8.3 卸载策略

- 退出分析页（onCleared）→ 延迟 30 秒后关闭 `Interpreter`（避免频繁进出重新加载）
- 内存警告 `onTrimMemory(TRIM_MEMORY_RUNNING_LOW)` → 立即关闭所有 Interpreter
- 首页不持有模型引用

---

## 9. V3 架构演进：AI 服务 + 食材百科 + 个性化饮食（2026 年 6 月）

### 9.1 V3 架构概览

V3 在 V2 的本地推理管线基础上新增了三个独立模块：

- **AI 服务层**（`data/ai/`）：`QwenAIService` 封装 DashScope API
- **食材百科引擎**（`data/produce/`）：`ProduceInfoEngine` 两阶段 Flow
- **饮食计划引擎**（`data/diet/`）：`DietPlanEngine` BMR+TDEE+AI 生成

V3 架构保持 Clean Architecture 分层不变，domain 层仍为纯 Kotlin（无 Android 依赖），新增的 `ResourceProvider` 和 `UriInputStreamProvider` 接口进一步解耦 ViewModel 对 Android Context 的依赖。

### 9.2 新增路由与页面

| 路由 | Screen | 版本 | 说明 |
|------|--------|------|------|
| `personalize` | PersonalizeScreen | V3 新增 | 健康画像表单（15+ 字段） |
| `diet-plan` | DietPlanScreen | V3 新增 | 7 天饮食计划查看 |
| `profile/taste` | TasteProfileScreen | V3 废弃 | 自动重定向到 personalize |

BottomNavigation 保持 3 Tab（Home/History/Settings），新页面通过 Home 卡片或 Settings 入口导航进入。

### 9.3 新增数据模型

#### Domain 层

- `ProduceInfo`：食材信息（核心营养 + AI 扩展 + 缓存时间戳）
- `DietPlan`：饮食计划（7 天 JSON + 生成时间 + 热量目标）
- `DailyMealPlan`：单日餐计划（早餐/午餐/晚餐/加餐 + 营养汇总）
- `Meal`：单餐（菜名 + 食材 + 步骤 + 热量/蛋白/碳水/脂肪）
- `UserProfile`：用户画像（口味 + 身体指标 + 活动水平 + 健康目标 + 限制）

#### Data 层 - Room

- `UserProfileEntity`：单例表（CHECK(id=1)），存储完整健康画像 JSON
- `DietPlanEntity`：多记录表，generatedAt 索引，90 天自动清理
- Room 版本：v2 → v3（新增表）→ v4（CHECK 约束迁移）

#### Data 层 - AI 服务

- `AIService` 接口：`chat(systemPrompt, userMessage)` + `chatJson(systemPrompt, userMessage, jsonSchema)`
- `QwenAIService` 实现：OkHttp 4.12.0 + DashScope API
- `AIServiceError` sealed class：6 种错误类型

### 9.4 两阶段 Flow 模式（ProduceInfoEngine）

```
调用 getInfo(label)
  ├─ Stage 1（同步，<1ms）：emit 本地核心信息
  │   └─ 从 produce_info.json 加载营养/健康/储存/时令
  └─ Stage 2（异步，~2s）：emit AI 扩展信息
      └─ QwenAIService.chatJson → 选购技巧/搭配建议/趣味知识
```

UI 层通过 `collect` 接收两次更新：第一次立即显示基础信息，第二次异步补充 AI 内容。LRU 缓存（50 条）命中时跳过 Stage 2。

### 9.5 饮食计划生成管线（DietPlanEngine）

```
用户画像 → Mifflin-St Jeor BMR → × 活动系数 = TDEE → ± 目标调整 = 每日热量
    ↓
构建结构化 Prompt（含口味/过敏/排除/类别偏好）
    ↓
QwenAIService.chatJson → 7天餐计划 JSON
    ↓
解析 → 写入 Room diet_plans 表
    ↓
插入前自动清理 90 天旧数据（deleteOlderThan）
```

### 9.6 新增依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| OkHttp | 4.12.0 | AI 服务 HTTP 客户端 |
| org.json | Android 内置 | JSON 构建与解析 |

APK 体积影响：+约 800KB（OkHttp + 依赖），总计 ~19MB。

### 9.7 V3 错误处理扩展

| 场景 | 处理策略 |
|------|----------|
| AI 服务超时（15s 连接 / 30s 读取） | 返回 TimeoutError，UI 提示"AI 响应超时" |
| 网络不可用 | 返回 NetworkError，ProduceInfo 回退到本地数据，DietPlan 提示联网 |
| API Key 无效（401/403） | 返回 AuthenticationError，提示检查密钥配置 |
| 配额耗尽（429） | 返回 QuotaExceeded，提示稍后重试 |
| AI 返回格式异常 | 返回 InvalidResponse，chatJson 自动去除 markdown 代码块标记 |
| 网络瞬态错误 | 自动重试 1 次，重试失败返回 NetworkError（保留原始异常） |

### 9.8 线程安全策略

| 组件 | 策略 |
|------|------|
| QwenAIService | OkHttp 连接池线程安全，无状态设计 |
| ProduceInfoEngine | `@Volatile` + `synchronized` LRU 缓存访问 |
| DietPlanEngine | 无共享状态，每次生成独立计算 |
| TFLiteClassifier | `@Volatile` interpreter + `synchronized` 加载/关闭 |
| EfficientDetEngine | `synchronized(loadLock)` 保护 detect/close |
| UserProfileDao | Room 内部线程安全，单例 CHECK 约束 |

---

> **文档状态**: v2.4 已审查（C1-C3/M1-M5/m1-m4 已修正，详见 05-文档审查报告-v2.md）；V3 章节已追加（2026-06）
